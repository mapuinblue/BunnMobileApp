import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mapuinblue.bunnmobileapp',
  appName: 'Bunn Productivity Mobile',
  webDir: 'dist'
};

export default config;